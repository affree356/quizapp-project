import 'package:flutter/material.dart';

class generalKnowledge extends StatelessWidget {
  const generalKnowledge({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('GK'),),
    );
  }
}